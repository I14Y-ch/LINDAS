name: "[TEST] Incremental Update"

on:
  # TODO Sergiy: decomment once the workflows are approved
  # schedule:
  #   - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  update-concepts:
    uses: ./.github/workflows/reusable_workflow.yml
    with:
      CLEAR_GRAPH: 'false'
      STARDOG_URL: 'https://stardog-test.cluster.ldbar.ch/lindas'
      LINDAS_QUERY_URL: 'https://test.lindas.admin.ch/query'
      BASE_API_URL: 'https://api-a.i14y.admin.ch/api/public/v1/concepts/'
      BATCH_SIZE: '10'
    secrets:
      STARDOG_PASSWORD: ${{ secrets.STARDOG_PASSWORD_TEST }}
      STARDOG_USER: ${{ secrets.STARDOG_USER }}